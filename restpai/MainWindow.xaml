<Window x:Class="Lab3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Введите логин и пароль" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин:" VerticalAlignment="Center" Margin="10"/>
        <TextBox x:Name="Login" Margin="10" Grid.Row="0" Grid.Column="1" Width="200" />

        <TextBlock Text="Пароль:" VerticalAlignment="Center" Margin="10" Grid.Row="1"/>
        <PasswordBox x:Name="Password" Margin="10" Grid.Row="1" Width="200" />

        <Button Content="Войти" Margin="10" Grid.Row="2" Click="Button_Click" Width="200"/>
    </Grid>
</Window>